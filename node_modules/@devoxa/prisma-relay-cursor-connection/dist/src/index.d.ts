import { Connection, ConnectionArguments, Edge, Options, PrismaFindManyArguments } from './interfaces';
export * from './interfaces';
export declare function findManyCursorConnection<Record = {
    id: string;
}, Cursor = {
    id: string;
}, Node = Record, CustomEdge extends Edge<Node> = Edge<Node>>(findMany: (args: PrismaFindManyArguments<Cursor>) => Promise<Record[]>, aggregate: () => Promise<number>, args?: ConnectionArguments, pOptions?: Options<Record, Cursor, Node, CustomEdge>): Promise<Connection<Node, CustomEdge>>;
